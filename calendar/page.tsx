// Calendar: classes, assessment tasks, self/AI tasks. Kanban view is a fast-follow (Sprint 5).
export default function CalendarPage() {
  return <main>{/* TODO: calendar view over `calendar_events` */}</main>;
}
